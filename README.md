# Soundgram Playlist Service

Микросервис на **FastAPI**, который вытаскивает инфу о плейлистах и треках. Он понимает разные форматы ссылок (старые и новые) и отдает красивый JSON, чтобы фронтенду было удобно рисовать интерфейс.

## 🛠 Технологический стек
* **Python 3.13** (используем свежий слим образ в Docker)
* **FastAPI** — чтобы API был быстрым и асинхронным.
* **Httpx** — чтобы ходить за данными асинхронно.
* **Pydantic** — чтобы данные были строго типизированы.
* **Docker** — для упаковки приложения в контейнер и запуска в любой среде.

---

## 🚀 Как запустить

### 1. Клонирование проекта
```bash
git clone https://github.com/Maur1ck/soundgram-service.git
cd soundgram-service
```

### 2. Запуск через Docker (Рекомендуется)

**Соберите образ и запустите:**
```bash
docker build -t soundgram-api .
docker run -p 8000:8000 soundgram-api
```
Сервис будет доступен тут: [http://localhost:8000/docs](http://localhost:8000/docs)

### 3. Запуск локально (Для разработки)

**Установите зависимости и запустите:**
```bash
python -m venv .venv
source .venv/bin/activate  # или .venv\Scripts\activate на Windows
pip install -r requirements.txt
uvicorn app.main:app --reload
```

---

## 🧪 Тестирование

В проекте написаны Unit-тесты для логики парсинга и интеграционные тесты для API.
Для запуска тестов используйте команду:
```bash
pytest
```

---

## 🔌 Как использовать API

Нужно отправить **POST** запрос на `/playlist`. В теле запроса передаем `url` ссылки на плейлист.

**Пример запроса:**
```json
{
  "url": "https://music.yandex.ru/users/misha20061508/playlists/1001"
}
```

**Пример ответа:**
```json
{
  "title": "Most Likes",
  "owner": "Миша Мишин",
  "tracks": [
    {
      "title": "Save Your Tears",
      "authors": [
        "The Weeknd",
        "Ariana Grande"
      ],
      "cover_url": "https://avatars.yandex.net/get-music-content/4716681/6ad252e9.a.15120187-1/400x400",
      "iframe_html": "<iframe frameborder=\"0\" style=\"border:none;width:100%;height:180px;\" width=\"100%\" height=\"180\" src=\"https://music.yandex.ru/iframe/album/15120187/track/81581652\"></iframe>"
    }
  ]
}
```

---

## 🧠 Assumptions & Edge Cases

### 1. Получение данных
Вместо сложного парсинга HTML-страниц (который часто ломается), я использую **сторонние эндпоинты** для получения сырых данных в формате JSON. Это намного быстрее и надежнее.
* **Риск:** Эти эндпоинты могут изменить формат ответа.
* **Решение:** Весь парсинг данных вынесен в отдельный сервис. Если формат изменится, поправить нужно будет только один файл.

### 2. Форматы ссылок
Я поддержал два основных формата ссылок:
* Старые: `users/{owner}/playlists/{kind}`
* Новые: `playlists/{uuid}`
Сервис сам определяет формат через регулярные выражения и выбирает нужный источник данных.

### 3. Обработка ошибок
* **Капча:** Если внешний источник решит, что мы бот, сервис вернет **403 Forbidden**.
* **Нет доступа:** Если плейлист приватный или его не существует — вернем **404 Not Found**.
* **Таймауты:** Если внешний сервис тормозит, мы ждем не более 15 секунд, чтобы не вешать наше приложение, и отдаем **503**.

### 4. Генерация плеера
Код для встраивания (`iframe`) генерируется на лету по ID трека и альбома. Это избавляет от необходимости делать лишние запросы к страницам треков.

---

## 🔮 Что бы я сделал за +2-3 часа

1.  **Ретраи (Retry):** Чтобы мелкие сетевые сбои не приводили к ошибкам.
2.  **Кэш (Redis):** Чтобы одни и те же популярные плейлисты отдавались мгновенно.
3.  **Логирование:** Чтобы в реальном времени видеть, когда внешние источники начинают отдавать ошибки.

---
